services:
  homepage:
    extends:
      file: composes/homepage.yaml
      service: homepage
  nginx:
    extends:
      file: composes/nginx.yaml
      service: nginx
networks:
  test_network:
    driver: bridge
    name: test_network
    ipam:
      config:
      - subnet: 172.20.0.0/24
        gateway: 172.20.0.1
